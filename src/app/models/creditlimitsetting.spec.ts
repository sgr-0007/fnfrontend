import { Creditlimitsetting } from './creditlimitsetting';

describe('Creditlimitsetting', () => {
  it('should create an instance', () => {
    expect(new Creditlimitsetting()).toBeTruthy();
  });
});
